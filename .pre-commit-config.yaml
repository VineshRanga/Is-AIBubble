repos:
  - repo: local
    hooks:
      - id: block-secrets-and-data
        name: Block .env and data/ from staging
        entry: python scripts/check_staged_secrets.py
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit]
